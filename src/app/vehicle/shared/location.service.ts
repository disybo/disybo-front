import { Injectable } from '@angular/core';
//import { BehaviorSubject } from "rxjs/Rx";
//import { Observable } from 'rxjs/Observable';
//
//import { StaraService } from '../../core/stara.service';
//import { VehicleLocation }  from './vehiclelocation.model'


@Injectable()
export class LocationService {//extends StaraService {
//  private _location: BehaviorSubject<VehicleLocation> = new BehaviorSubject(new VehicleLocation());
//
//get location(): Observable<VehicleLocation> {
//  return new Observable(fn => this._location.subscribe(fn));
//}
//
//public updateLocation(vehicle_id: string): void {
//  let date = new Date();
//  this.getLocation(vehicle_id, date.getTime(), date.getTime() - 10000).subscribe(data => {
//      console.log(data);
//      this._location.next(data);
//  });
//}

}
